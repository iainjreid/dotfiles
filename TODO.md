* [ ] Move the Vim packages into "opt", and move the relevant "packadd"
  command(s) into the relevant filetype blocks.

* [ ] Split out Vim configuration into its own repository that can be
  included as a package in its own right.
